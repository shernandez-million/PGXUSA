# PGX — the expert team

The persisted expert roster for this repository, one file per agent in
`.claude/agents/`. Constituted 2026-09-01 by APEX (Sub-APEX adaptation pass) on
Andrés's direct order to apply APEX to every running project — Método Muñoz §2:
the team is constituted before building, and it persists and grows from one
requirement to the next. This file mirrors that directory 1:1; if they ever
disagree, the directory is reality and this file is the defect.

## Roster (wave 1 — 8 agents)

| Agent | Domain | Hardest veto |
|---|---|---|
| `business-strategist` | High-end positioning, Miami-Dade & Broward only, site_plan.json facts | An area added "because it would rank" |
| `claims-warden` | Claims law (CLAUDE.md §1): allowlist, no numbers on inspection/permit claims, no third-party guarantees | Any number on an inspection clock or review time |
| `brand-guardian` | Design law (CLAUDE.md §3): Marcellus/Inter, scrims, hero, wordmark pipeline | A tinted scrim or a font-drawn wordmark |
| `seo-architect` | 524-URL architecture, entity graph, atomic bilingual hreflang/sitemap | A page without its language twin |
| `es-transcreator` | Miami Spanish transcreation, usted, calque hunting, fact fidelity | A calque or an invented fact in either direction |
| `build-engineer` | build.py / make_template_es.py / audit.py, template sync, markers | Hand-editing template-es.html or weakening a count check |
| `performance-engineer` | CLS, Lighthouse ≥95 / 100 / 100 / 100, fonts, image weight | A text container sized in `ch` |
| `production-qa` | Definition of done, live-URL verification, mobile 390/430pt | "Done" without a fresh load of www.pgxusa.com |

## How the team works here

- **Every requirement convenes its minimum qualified group** from this roster
  before implementation (Método Muñoz §2); a valid review invokes the agents for
  real and cites their verdicts — declaring "reviewed" without running them does
  not count.
- **Universal experts are account-level, not duplicated here**: the prompting
  expert (`~/.claude/agents/prompt-architect.md`) decodes every instruction from
  Andrés into a meta-prompt before execution; the Claude/orchestration expert is
  instantiated ad-hoc per session. The critics' jury (Método §2) reviews every
  deliverable bound for Andrés and is convened per pass, drawing on this roster.
- **Content changes** always pass `claims-warden` + `es-transcreator` (both
  languages, same pass). **Visual changes** always pass `brand-guardian` +
  `performance-engineer`. **Everything** converges on `production-qa` before it
  is called done.

## History

- **2026-09-01 — Team constituted (wave 1).** APEX Sub-APEX pass on Andrés's
  order («aplicar APEX a todos los proyectos corriendo»). No prior roster
  existed. Eight agents derived from the repo's own law (CLAUDE.md §§1–5) — each
  charter encodes corrections already paid for in real incidents (BORA, the `ch`
  CLS, the four scrim corrections, the 640-correction ES audit, the stale-deploy
  false failure). Jury completeness check applied at constitution: accessibility
  is owned by `performance-engineer` (the 100 a11y bar) with `production-qa`
  enforcing it; conversion/form work is Andrés-gated (CLAUDE.md §4) so no
  dedicated agent until he opens that door; imagery generation belongs to
  `brand-guardian`. Redundancy check: claims vs positioning kept separate (they
  veto for different reasons — legality of a sentence vs fit of a market);
  performance vs QA kept separate (a metric bar vs a verification ritual). Wave
  2 of the chain reaction (each agent maps its sub-domains and proposes the next
  wave's experts) is pending its jury gate per APEX doctrine.

## Design squad (wave 2 — Flagship per-project adaptation, 2026-09-02)

Constituted on Andrés's order of 2026-09-02 («sin importar el proyecto, Flagship
se adapte al proyecto y busque los mejores expertos en diseño, en UI, en UX, en
branding… la idea es que todos nuestros productos sean lo más fáciles de usar y
tengan un diseño impecable»). This is the repo's OWN design squad for its sector
— high-end residential remodeling, programmatic bilingual landings, luxury
interior photography, conversion to a consultation. It does not duplicate the
account-level Flagship body (`flag-*`, 23 transversal seats in Project 120): they
audit from the jury; these design inside the repo. CLAUDE.md §3 remains the design
law and `brand-guardian` its enforcer. The four charters passed the Flagship
wave jury (2026-09-02, fixes applied) before landing here.

**Convocation rule:** every design request convenes the whole design squad below
+ the Flagship verticalist `flag-vertical-*` + the taste jury (skill `flagship`,
account-level — never committed here, CLAUDE.md §4). For this repo the verticalist
is `flag-vertical-corporativo` (skill `flagship` §5, row «Servicios locales y civil
(PGX…)» — the company lens); `flag-vertical-ultralujo` joins only when the brief
declares the premium-residential lens in one line, never by default.
`luxury-remodeling-designer` is the sector lens; a home-services verticalist in
Project 120 is an open chain-reaction item, not a reason to pick another one.
Protocol is the skill's: signed brief → ≥3 orthogonal directions → decision
meeting → 3–5 navigable options to Andrés in ONE URL with one recommendation.
Internal authority stays with the repo (Sub-APEX): `brand-guardian` signs design
law, `production-qa` signs done.

| Agent | Design corner | Hardest veto | Origin |
|---|---|---|---|
| `brand-guardian` | Creative direction and design law (CLAUDE.md §3): tokens, Marcellus/Inter, scrims, hero composition, photo rules, wordmark pipeline and propagation | A tinted scrim or a font-drawn wordmark | Wave 1 |
| `business-strategist` | Register of every visible line and CTA against the high-ticket filter; catalog of areas/services/types | Budget-market language; an area added «because it would rank» | Wave 1 |
| `claims-warden` | Every string on a design surface is a claim: allowlist, no numbers on inspection/permit, no third-party guarantees | Any number on an inspection clock or review time | Wave 1 |
| `es-transcreator` | The Spanish twin of every design surface: usted, no calques, fact fidelity | A calque, tuteo, or an English survival on an ES page | Wave 1 |
| `seo-architect` | The URL space the design lives in; titles/metas/headings; atomic bilingual | A page without its language twin | Wave 1 |
| `build-engineer` | Template ↔ generated ES template ↔ hand-kept homepages; asset generators; audit guards | Hand-editing `template-es.html`; a `--force` publish | Wave 1 |
| `performance-engineer` | CLS, fonts, image weight, Lighthouse ≥95 / 100 / 100 / 100 | A text container sized in `ch` | Wave 1 |
| `production-qa` | Definition of done: live URL loaded fresh, mobile 390/430 by scrolling, no horizontal scroll | «Done» without a fresh load of www.pgxusa.com | Wave 1 |
| `luxury-remodeling-designer` | **NEW.** Sector seat: the design grammar of the luxury design-build category — first-screen answer (service, area, who, licensed), proof order, process visible; writes and signs the sector-benchmark section of every design brief | The directory clone (rating widgets, badge rows, "get 3 quotes", calculators, urgency) or a rendering staged as built work | Wave 2 · 2026-09-02 |
| `consultation-path-designer` | **NEW.** UX of the path from Google result to estimate: reading order, CTA hierarchy (hero solid + ghost, dock Call/WhatsApp/Estimate), form fields, states, error and mailto fallback, in both twins and all three form copies | The path that dies: silent submit, hidden fallback, a required field beyond name + one channel; any backend wired without Andrés | Wave 2 · 2026-09-02 |
| `photo-director` | **NEW.** Art direction of every image: six service heroes, twelve detail crops, seven OG cards, the empty portfolio, the shot-list brief for Andrés's real project photos | A rendering or stock staged as built work; a crop that reads as texture; a hero that needs a heavier scrim to be legible | Wave 2 · 2026-09-02 |
| `accessibility-warden` | **NEW.** What Lighthouse does not measure (CLAUDE.md §3): keyboard drive per page class, skip link first, `:focus-visible` survival, named toggles, pixel-measured contrast over photos, 44 px targets on dock/chips, reduced-motion, both twins | «Done» on a page class never driven by keyboard; `outline:none` on `:focus` without the restore; a contrast fix by scrim | Wave 2 · 2026-09-02 |

Neighbours the squad convenes by surface (not design seats, but no package reaches
Andrés without them when their surface appears): `flag-marketing-conversion`
(only the day paid traffic lands — today no campaign and no Google Business Profile,
CLAUDE.md §6), `flag-craft-*` (transversal audit of every direction, lens by lens),
`flag-jurado-gusto` + `flag-scorer-diseno` ×3 + `flag-arbitro-gusto` (the jury),
`flag-registrador-vetos` (every rejection or correction by Andrés becomes law the
same day — RV in the account, and its rule appended to CLAUDE.md §3 the same
session, per this file's own rule).

### How the team works here — addendum (2026-09-02)

- **Design requests** (a new page class, a redesign, options for Andrés, a
  correction of his on any surface) convene the design squad above in full,
  the Flagship verticalist and the taste jury — one brief carries every section,
  one line carries one signature; `brand-guardian` remains the internal design
  authority and `production-qa` the gate.

### History — addendum (2026-09-02)

- **2026-09-02 — Design squad constituted (wave 2).** Flagship per-project
  adaptation on Andrés's order of the same day. Four seats derived from the
  corners the wave-1 jury check left open or assigned to a metric that cannot
  see them: the sector's design grammar (nobody held the directory-clone veto),
  the consultation path (wave 1 deferred it as «Andrés-gated» — the backend
  still is, CLAUDE.md §4; the path's UX, fields, states and fallback are live
  today in three hand-kept copies and now have an owner who drafts backend
  proposals for him and never wires one), photography direction (brand-guardian
  enforces the photo law; choosing, cropping and briefing had no seat, and every
  image is a rendering awaiting real projects), and the keyboard law CLAUDE.md §3
  added on 2026-09-01 after the roster was constituted (assigned to the a11y
  100 score that, by CLAUDE.md's own record, does not measure it). Considered
  and dropped, with the reason on file in the account dossier `pgx` §5:
  UI-systems (same veto as `flag-craft-ui`; the 133-line CSS drift between
  `index.html` and `template.html` is a `build-engineer` guard to add, not a
  design seat), copy chief, typographer, motion, mobile, branding strategist,
  identity applicator, locale-layout, QS/paid-conversion. Chain reaction open
  for the repo session: extend `build-engineer`'s sync guard from the form to
  the shared CSS of the two homepages; `production-qa`'s mobile pass named
  explicitly on both twins; a `flag-vertical-` seat for local home services in
  Project 120. Landed on branch `flagship/escuadra-diseno-2026-09-02` (worktree
  `.worktrees/flagship-escuadra`, FLOTA.md: repo without an active session) —
  this commit touches only `.claude/agents/`, `TEAM.md` and `.gitignore`; the
  two «eight agents» counts in CLAUDE.md (§4 cloud-session note, §7) become
  «twelve» in the repo session's next pass.
